branches:
  only:
    - master
language: python
python:
  - "2.7"
before_install:
  - pip install -U pip
install:
  - python -m pip install --upgrade pip
  - pip install --upgrade -r requirements.txt
script:
  - ./scripts/test.sh